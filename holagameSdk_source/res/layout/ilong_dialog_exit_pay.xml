<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
 >

    <RelativeLayout
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:background="@drawable/ilong_square_radius" >

        <TextView
            android:id="@+id/ilong_dialog_base_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingBottom="40dp"
            android:paddingTop="40dp"
            android:text="您的订单未完成，是否结束支付？"
            android:textColor="#646464"
            android:textSize="18sp" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1px"
            android:layout_below="@id/ilong_dialog_base_message"
            android:background="#949494" />

        <LinearLayout
            android:layout_below="@id/ilong_dialog_base_message"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:orientation="horizontal" >

            <Button
                android:id="@+id/ilong_dialog_base_canel"
                android:layout_width="match_parent"
                android:layout_height="fill_parent"
                android:layout_weight="1"
                android:text="确认结束"
                android:textColor="#949494"
                android:background="@drawable/ilong_radius_left_withe_bg"
                android:textSize="18sp" />

            <Button
                    android:id="@+id/ilong_dialog_base_positive"
                android:layout_width="match_parent"
                android:layout_height="fill_parent"
                android:layout_weight="1"
                android:text="继续支付"
                android:background="@drawable/ilong_radius_right_yellow_bg"
                android:textColor="@android:color/white"
                android:textSize="18sp" />
        </LinearLayout>
    </RelativeLayout>

</RelativeLayout>